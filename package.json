{
  "name": "timestamp",
  "version": "1.0.0",
  "description": "Simple timestamp API server",
  "main": "index.js",
  "scripts": {
    "preinstall": "cd ui && npm i",
    "prestart": "rm -r public && cd ui && npm run build && mv dist/demo/browser ../public/",
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  }
}
